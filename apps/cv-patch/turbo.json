{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": [".output/**", ".vercel/output/**"],
      "env": ["VITE_CONVEX_SITE_URL", "VITE_CONVEX_URL",]
    }
  }
}
